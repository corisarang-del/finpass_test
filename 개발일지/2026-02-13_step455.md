# 개발일지 Step 455
- 작성시각: 2026-02-13 16:53

## 해결하려던 문제
- 페이지별 가이드 톤/이름이 완전히 통일되지 않던 문제
- 결과 값이 온보딩 답변을 충분히 반영하지 못하던 문제
- 가이드 코멘트 문체가 반말로 보이던 부분을 존댓말로 바꿔야 하는 문제

## 해결된 것
- 공통 로직 파일 `src/lib/plannerProfile.ts` 추가
  - 카테고리별 기본 입력값 정의
  - 온보딩 답변 기반 초기 재무값 추론 로직 추가
  - `Result/Simulation/Final`용 가이드 코멘트 생성 함수 추가(존댓말)
  - 가이드 프로필(에이전트 한/송/최/유) 통일
- `Result.tsx`
  - 온보딩 답변 기반 초기값 자동 적용
  - 카테고리/답변별 존댓말 가이드 멘트 표시
- `LiveSimulation.tsx`
  - 답변 기반 입력값 자동 반영
  - 가이드 코멘트 로직을 공통 유틸 기반으로 변경(존댓말)
  - 가이드 이름/역할 표기 통일
- `FinalAnalysis.tsx`
  - 최종 가이드 멘트 공통 유틸 기반으로 변경(존댓말)
  - 카테고리/선택값 반영된 멘트 출력
- eslint 검증 통과

## 해결 안 된 것
- 전체 `pnpm build`의 기존 타 파일 이슈(이번 범위 외)는 처리하지 않음
