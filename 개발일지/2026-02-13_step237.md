
# 개발일지 - 2026-02-13_step237

## 작성시각
2026-02-13 13:52

## 해결하고자 한 문제
`LiveSimulation.tsx` 런타임 에러 (`SimulationInputs` export 없음) 및 다수의 컴포넌트 Props 불일치 린트 에러.

## 해결된 것
- `LiveSimulation.tsx`: `import type { SimulationInputs }`로 분리.
- `LiveChart.tsx`: `targetMonthlyExpense` prop 추가 (인터페이스만 수정, 구현은 Recharts에서 사용 안하면 무시됨).
- `ExpertPopup.tsx`: `isVisible`, `onClose` prop 추가.
- `ActionPlanCard.tsx`: `icon`, `color` prop 추가.

## 해결 안 된 것
- `LiveChart.tsx` 내부에서 `targetMonthlyExpense`를 실제로 렌더링에 사용할지 여부 (일단 타입 에러 해결 우선).
- `ActionPlanCard.tsx` 내부에서 `icon`, `color` 렌더링 로직 추가 필요 (기존 코드가 이 prop을 안 쓰고 있을 수 있음).
