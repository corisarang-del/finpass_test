# Step 464 개발일지

- 작성시각: 2026-02-13 18:31:06
- 해결하고자 한 문제:
  - `pnpm run build`에서 TS6133/TS6196 미사용 심볼 오류 6건으로 빌드가 중단되는 문제 해결.

- 해결된 것:
  - `CompoundInterestCard.tsx`: 미사용 `Coins` import 제거.
  - `ExpertPopup.tsx`: 미사용 `ArrowRight` import 제거.
  - `LiveChart.tsx`: 미사용 props(`currentAge`, `targetMonthlyExpense`) 구조분해 제거.
  - `MoneyInput.tsx`: 미사용 `step` 구조분해 제거.
  - `useExpertAdvisor.ts`: 미사용 타입 `SimulationInputs` import 제거.
  - `pnpm exec tsc -b` 통과 확인(타입 에러 0건).

- 해결되지 않은 것:
  - `pnpm run build`는 타입 에러가 아니라 Vite 실행 단계에서 `spawn EPERM`으로 실패.
  - 현재 오류는 코드가 아니라 로컬 실행 환경/권한(프로세스 spawn) 이슈로 보이며, 별도 환경 조치가 필요.
