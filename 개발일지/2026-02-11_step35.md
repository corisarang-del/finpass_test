# 개발일지 - 2026-02-11

작성시각: 2026-02-11 15:05
해결하고자 한 문제:
- `pnpm` 실행 시 `IMPORTER_MANIFEST_NOT_FOUND` 오류 해결
- 사용자가 생성한 `index.html` 오타 수정
- `App.tsx` 문법 오류 수정

해결된 것:
- `index.html` 태그 오타 제거
- `App.tsx` 중복된 `export default` 제거
- 사용자에게 올바른 실행 경로(`finpass-web`) 안내 예정

원인:
- 프로젝트 루트(`asset_up`)에서 `pnpm` 명령어를 실행하여 발생한 오류. 실제 프로젝트는 `finpass-web` 하위에 있음.
