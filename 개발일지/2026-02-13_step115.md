
# 개발일지 - 2026-02-13_step115

## 작성시각
2026-02-13 12:49

## 해결하고자 한 문제
LiveSimulation 페이지 구현 후 발생한 린트 에러(Type Import, 미사용 변수) 수정 및 최종 코드 폴리싱.

## 해결된 것
- `ExpertPopup.tsx`, `LiveChart.tsx`: `import type` 적용으로 빌드 경고 해결
- `ActionPlanCard.tsx`: `image` prop을 은은한 배경 이미지로 활용하도록 개선
- `LiveSimulation.tsx`: `ExpertPopup`의 표시 여부를 `showPopup` 상태로 제어하도록 로직 연결

## 해결 안 된 것
- 없음. (테스트 필요)
