# Step 467 개발일지

- 작성시각: 2026-02-13 18:50:28
- 해결하고자 한 문제:
  - Vercel 배포 주소 접근 시 `404: NOT_FOUND`가 발생하는 문제 해결.

- 해결된 것:
  - `finpass-web/vercel.json` 파일 추가.
  - Vercel 설정 명시:
    - framework: vite
    - installCommand: pnpm install
    - buildCommand: pnpm build
    - outputDirectory: dist
    - rewrites: `/(.*)` -> `/index.html`
  - SPA 라우팅 경로 직접 접근 시 404가 발생하지 않도록 rewrite 적용.

- 해결되지 않은 것:
  - Vercel 대시보드의 Root Directory가 `finpass-web`가 아니면 여전히 404가 날 수 있음.
  - 대시보드 설정 반영 후 재배포 필요.
