# 개발일지 - 2026-02-11 (Cinematic Home Redesign)

작성시각: 2026-02-11 16:25
해결하고자 한 문제:
- 기존의 단순 텍스트 애니메이션이 아닌, "Andreas Antonsson" 스타일의 감각적인 풀스크린 이미지 트랜지션 웹사이트로 업그레이드 요청.
- 3단계 텍스트에 어울리는 감성적인 배경 이미지 적용 필요.

변경 사항:
- `Home.tsx` 구조 변경:
    - `div` (Background Layer): 3개의 이미지를 `AnimatePresence`로 교체하며 `opacity` 트랜지션 처리.
    - `div` (Content Layer): 텍스트와 CTA가 배경 위에 `z-index`로 올라가도록 구성.
- 이미지 소스 선정 (Unsplash ID 활용 예정):
    1. **Data/Stars**: `photo-1451187580459-43490279c0fa` (지구, 데이터, 네트워크 느낌) 또는 `photo-1534796636912-3b95b3ab5986` (별밤)
    2. **Path/Light**: `photo-1504283152331-15525d8868df` (숲길, 빛) 또는 `photo-1470252649378-9c29740c9fa8` (새벽 풍경)
    3. **Warm Future**: `photo-1470252649378-9c29740c9fa8` (따뜻한 들판) 또는 `photo-1500382017468-9049fed747ef` (황금빛 노을)

기술적 포인트:
- `framer-motion`의 `AnimatePresence`를 사용하여 이미지와 텍스트가 동시에 부드럽게 전환되도록 `mode='sync'` 또는 `wait` 조정.
- 모바일 대응을 위해 배경 이미지는 `object-cover`로 처리.
