# Prompt Log - 2026-02-11 18:30

작성시각: 2026-02-11 18:30
입력된 프롬프트:
"여전히 글자가 겹쳐서 나옴. 그리고 홈화면도 가운데에 있던 글자가 저렇게 왼쪽상단으로 겹쳐서 나옴"

수행한 작업:
1. **Hero Text Alignment Fix**:
    - "왼쪽 상단 겹침" 원인 분석: `absolute inset-0` 컨테이너가 제대로 된 높이/센터링 컨텍스트를 갖지 못했거나, 모션 컴포넌트 간의 충돌 가능성.
    - 해결: `z-40` 등 명확한 Z-Index를 부여하고, `flex items-center justify-center`를 `w-full h-full`과 함께 강제 적용하여 정중앙 배치 보장. Navbar(`z-50`)와의 충돌 방지.
2. **Category Text Overlap Fix**:
    - 원인: `absolute` 포지셔닝으로 배치된 설명글(`Description`)이 Title과 좌표상 겹치는 문제.
    - 해결: 
        - 텍스트 컨테이너를 `flex flex-col justify-end` (하단 정렬)로 변경.
        - Title과 Description을 일반 문서 흐름(Flow)에 배치하여 절대 겹치지 않게 함.
        - Hover 시 Title이 위로 올라가 공간을 만들면, Description이 그 자리에 자연스럽게 나타나도록(`max-h` 또는 `opacity` 트랜지션) 수정.
3. **Layout Stability**:
    - Flex 대신 Grid(`grid-rows-4` / `grid-cols-4`)를 사용하여 4개의 카테고리가 강제로 균등 분할되도록 수정 (하나가 안 보이는 문제 해결).
