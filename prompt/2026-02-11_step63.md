# Prompt Log - 2026-02-11 18:05

작성시각: 2026-02-11 18:05
입력된 프롬프트:
"마우스 올렸을시 텍스트가 겹쳐서 나옴. 그리고 카테고리 한개가 안보임."

수행한 작업:
1. **Overflow Issue Fix (카테고리 잘림)**:
    - 원인: Root Container에 `h-screen overflow-hidden`이 설정되어 있는데, 내부 콘텐츠(세로형 배치 시)가 화면 높이를 초과하여 4번째 카드가 잘림.
    - 해결: `overflow-hidden` -> `overflow-y-auto` (또는 main에 scroll 적용)로 변경하여 스크롤 가능하게 만듦.
2. **Text Overlap Fix (텍스트 겹침/줄바꿈)**:
    - 원인: Subtitle Badge("현실 점검" 등)가 좁은 공간에서 줄바꿈이 일어남(`flex` 이슈).
    - 해결: `whitespace-nowrap` 추가. `z-index` 재확인.
3. **Layout Logic**:
    - 데스크탑(`md`)에서도 창 크기가 작으면 `grid-cols-4`가 찌그러질 수 있으므로, 반응형 단계를 `lg:grid-cols-4`, `md:grid-cols-2` 등으로 완하시켜 한눈에 더 잘 들어오게 조정.
