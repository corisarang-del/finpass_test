# Prompt Log - 2026-02-11 17:15

작성시각: 2026-02-11 17:15
입력된 프롬프트:
"여전히 3번째 화면 카테고리 선택 카드가 보이질 않어. https://www.basedesign.com/ 이런 홈페이지처럼 아래로 스크롤화면 동영상이 커지는건 어떻게 구현하지"

수행한 작업:
1. **문제 분석**: 
    - 이전의 `flex-1` 기반 레이아웃에서 높이 계산 문제로 카드가 찌그러진 것으로 추정됨.
    - 사용자가 "Basedesign.com" 스타일의 **스크롤 인터랙션(Scroll-based Expansion)**을 구체적으로 요청함.
2. **`Home.tsx` 전면 재설계 (Scroll Interaction)**:
    - **Before**: 3단계 타이머 자동 슬라이드.
    - **After**: 사용자가 스크롤을 내리면 -> 중앙의 이미지가 꽉 찬 화면(Full Width)으로 확장됨 -> 그 후 카테고리 메뉴가 등장.
3. **구현 상세**:
    - `framer-motion`의 `useScroll`, `useTransform` 활용.
    - **Hero Section**: 스크롤에 따라 `width` 또는 `scale`이 커지는 Sticky Container 구현.
    - **Category Section**: 스크롤이 일정 이상 내려갔을 때 자연스럽게 나타나는 하단 섹션.
    - **이미지 교체**: 첫 번째 배경 이미지가 안 나오는 문제 해결을 위해 검증된 URL 사용.
