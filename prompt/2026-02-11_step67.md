# Prompt Log - 2026-02-11 18:45

작성시각: 2026-02-11 18:45
입력된 프롬프트:
"여전히 홈화면 왼쪽상단으로 글자 몰려서 겹침. error2.png보면 이미지가 절반만 나오게 하지말고 카테고리 메뉴마다 다 꽉차게 이미지 나오게 해줘"

수행한 작업:
1. **Hero Text Centering (Fixed Positioning)**:
    - 원인: `relative` 부모(`main`)가 플렉스 컨텍스트이나, 내부의 `absolute`가 예상대로 동작하지 않음 (특히 모션 라이브러리와 결합 시).
    - 해결: 텍스트 컨테이너를 `fixed inset-0 z-50 flex items-center justify-center`로 변경하여 부모 레이아웃과 무관하게 **뷰포트 정중앙**에 강제 고정.
2. **Category Full-Screen Layout (Flex Split)**:
    - 원인: Grid (`grid-rows-4`)가 특정 높이 컨텍스트에서 `auto`로 작동하여 콘텐츠 크기만큼만 잡히거나 찌그러짐.
    - 해결:
        - Grid 제거 -> `flex flex-col md:flex-row` 도입.
        - 각 아이템에 `flex-1`을 주어 **무조건 화면을 N등분**하여 꽉 채우도록 설정.
        - 컨테이너 자체도 `fixed inset-0`으로 설정하여 Navbar 높이 등을 무시하고 화면 전체를 덮어버림(몰입감 증대).
3. **Text Overlap & Styling**:
    - 텍스트 위치를 `absolute`가 아닌 Flex Flow (`justify-center`)로 관리하여 겹침 원천 차단.
    - Title과 Tag 사이의 간격을 조정.
