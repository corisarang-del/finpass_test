# 프롬프트 - 2026-02-11 (Step 74)

## 사용자 요청
```
이제 카테고리는 여백없이 잘보여. 그런데 홈화면은 마우스 움직이면 ps로고가 여전히 보이네.
```

## 수행한 작업

### 1. 원인 분석
- Adobe Creative Cloud 브라우저 확장 프로그램이 `<img>` 태그를 감지하여 PS 로고 표시
- CSS `background-image`는 확장 프로그램이 감지하지 못함

### 2. Home.tsx 수정

#### 인트로 배경 (54-68줄): img → background-image
```tsx
style={{
  backgroundImage: `url(${bgImage})`,
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  opacity: 0.6,
}}
```

#### 카테고리 이미지 (140-155줄): img → background-image
```tsx
style={{
  backgroundImage: `url(${cat.image})`,
  backgroundSize: 'cover',
  backgroundPosition: 'center',
}}
```

### 3. 문서 작성
- 개발일지: `개발일지/2026-02-11_step74.md`
- 프롬프트: `prompt/2026-02-11_step74.md`
